pipeline {
    agent any
    stages {
        stage('Minting Python') {
            steps {
                sh 'sudo yum install python3'
                  }
               }            
              }
            } 
