pipeline {
    agent any
    stages {
        stage('Minting Python') {
            steps {
                sh 'python3 --version'
                sh 
                  }
               }            
              }
            } 
